#Reserved words
#https://docs.python.org/3/reference/lexical_analysis.html#keywords

There are 35 reserved words in Python 3.10.2, which cannot be used as identifiers (variable names, function names, etc.):
    False      await      else       import     pass
    None       break      except     in         raise
    True       class      finally    is         return
    and        continue   for        lambda     try
    as         def        from       nonlocal   while
    assert     del        global     not        with
    async      elif       if         or         yield

1. Reserved words are case-sensitive, meaning that "True" is a reserved word, but "true" is not.
2. Some reserved words have special meanings in specific contexts. For example, "async" and "await" are used in asynchronous programming,while "def" is used to define functions.
3. All reserved words contain only alphabet symbols
4. Reserved words cannot be used as variable names, function names, or any other identifiers in Python code. Attempting to use a reserved word as an identifier will result in a syntax error.
5. The only reserved words contains upper case alphabet symbol are True , False , None



#Reserved Liternals

#There are 3 reserved literals in Python 3.10.2, which represent specific values that cannot be changed:
    True       False      None

